<div class="petition-container">
    <h1>Welcome to the Big Lebowski SQL Petition Page!</h1>
    <div class="petition-text">
        <p>If you wanna make the world a better place with just one signature you are on the right place!
            All you need to do is fill out the fields below, which you can do single handedly while holding
            a white russian in your other hand.
            Help us collect enough signatures and bring the Dude, his Dudeness, Duder or el Duderino back to the big screen!
        </p>
        <h3>In Dude we Trust!</h3>
    </div>

    <form class="sign-form" action="/petition" method="post">
        <input
                    class="first-name"
                    type="text"
                    name="first-name"
                    placeholder="First Name"
                    required
                    minlength="3"
                    maxlength="15"
                    pattern="[a-zA-Z0-9]+"
                />
                <input
                            class="last-name"
                            type="text"
                            name="last-name"
                            placeholder="Last Name"
                            required
                            minlength="3"
                            maxlength="15"
                            pattern="[a-zA-Z0-9]+"
                        />
        <br>

        <input class="hidden-field" type="hidden" name="hidden-field" value="signature">
        <canvas id="signature-field" width="600" height="100"></canvas>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <!-- pass the token to the template, put this in every single form and make sure you pass the token here -->

        <button class="submit-btn" type="submit" name="button">Abide</button>

    </form>

</div>

{{>signature}}
